[package]
name = "wgpui-platform"
description = "Platform entry shim for wgpui-component (replaces Zed gpui_platform on native)."
version = "0.6.1"
edition.workspace = true
publish = true
license = "Apache-2.0"
homepage = "https://github.com/Muktidaya/wgpui"
repository = "https://github.com/Muktidaya/wgpui"

[features]
test-support = ["wgpui/test-support"]

[dependencies]
wgpui = { workspace = true }

[lints]
workspace = true
